conky.config = {
    own_window = true,
    own_window_class = 'StellarCenter',
    own_window_type = 'desktop',

    own_window_transparent = true,
    own_window_argb_visual = false,
    own_window_argb_value = 0,

    double_buffer = true,
    use_xft = true,
    override_utf8_locale = true,
    font = 'CodeNewRoman Nerd Font Mono:size=14',

    update_interval = 1,

    alignment = 'top_middle',
    gap_x = 0,
    gap_y = 450,

    minimum_width = 100,
    maximum_width = 1000,

    draw_shades = false,
    draw_outline = false,
    draw_borders = false,

    default_color = '#d8d8d8',
};

conky.text = [[
${alignc}${font CodeNewRoman Nerd Font Mono:size=12}${color #a8a8a8}AHMEDABAD       DAVAO${font}
${voffset 0.5}
${alignc}${font CodeNewRoman Nerd Font Mono:size=24}${color #d8d8d8}${time %H:%M}   ${execi 1 TZ=Asia/Manila date +%H:%M}${font}
${voffset 1}
${alignc}${font CodeNewRoman Nerd Font Mono:size=16}${color #d8d8d8}WEATHER TEST${font}
]];
